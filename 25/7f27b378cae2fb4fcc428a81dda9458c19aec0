Review fix (Phase 5 conductor-review pass): PatternBuilderScene.handleCorrect onComplete now destroys the flown card shape after the Back.out snap (it was only removed from cardShapes array, leaving an orphaned display object until the next renderRound). Added regression assertion in navigation.test.ts correct-tap test: expect(getMockFn(cardShape.destroy)).toHaveBeenCalled(). Verified: Biome clean 51 files, navigation.test.ts 278/278.
