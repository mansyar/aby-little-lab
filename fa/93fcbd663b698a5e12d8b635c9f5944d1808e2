Task: Scaffold the GitHub Actions workflow — created .github/workflows/ci.yml with pull_request (opened/synchronize/reopened) + push to master triggers; ubuntu-latest; actions/checkout@v4; pnpm/action-setup@v4 pinned 11.7.0; actions/setup-node@v4 Node 22 with cache: pnpm (keyed on pnpm-lock.yaml); pnpm install --frozen-lockfile; sequential gates check -> CI=true test -> build -> validate-pwa. Task: Validate workflow syntax & semantics — actionlint 1.7.12 reports 0 errors; PR path runs gates only (no deploy job at this phase); gate commands identical to Phase 1 baseline (all green). Deviation documented in tech-stack.md: pnpm/action-setup@v4 instead of raw corepack (same 11.7.0 pin).
