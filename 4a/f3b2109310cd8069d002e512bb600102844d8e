Task: Implement profile v2 storage (migration + per-profile stickers) — Added pure logic module src/game/profileLogic.ts (createDefaultStickers, createDefaultProfile, migrateV1, normalizeV2, addProfile, deleteProfile, switchActiveProfile, availableAvatarIds) and rewrote src/utils/storage.ts as a thin v2 facade: readV1/readV2/ensureV2/mutate, load() returns active profile stickers + global settings, earnSticker/hasSticker/resetProgress operate on active profile, new getProfiles/getActiveProfile/addProfile/deleteProfile/switchProfile/getAvailableAvatars; v1 key read-but-never-destroyed migration (additive, safe rollback); added ~150 lines of tests (profileLogic.test.ts new; storage.test.ts profile describe; updated AudioManager + firstWordsIntegration legacy tests for v2 key). Red phase confirmed 13 failing before implementation; Green: 813 tests pass; biome check clean (16 pre-existing warnings only). Files: src/game/profileLogic.ts, src/utils/storage.ts, src/types/index.ts, 4 test files. Why: Phase 1 Tasks 3-5 of multi-kid-profiles track.
