Task: Apply review suggestions for track 'Multi-Kid Profiles' (chip refresh) — Review finding: Hub avatar chip showed a stale texture after switching profiles via the picker or after add/delete in Settings (shelf re-rendered, chip did not). Added refreshProfileChip() in HubScene (re-textures chip from getActiveProfile), called from the picker switch handler and from the SettingsPanel onProgressReset callback. Regression tests: picker switch test asserts chip.setTexture('animal_cat') after switching back; new test simulates Settings addProfile + callback and asserts chip.setTexture('animal_dog'). 828/828 tests pass, biome clean (16 pre-existing warnings). Files: src/scenes/HubScene.ts, src/__tests__/scenes/navigation.test.ts. Why: review fix (Medium — active-profile identity chip must reflect the active profile).
