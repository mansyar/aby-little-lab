Task: Fix input lock persisting across scene re-launch in remaining affected scenes. Manual verification of the word-pool track surfaced that AlphabetScene and PatternBuilderScene share the same stale-inputLocked pattern as the first-words scenes; MusicalMemoryScene was also audited. Regression tests (TDD): re-launch after completion via create() on the same instance must unlock input and register a tap (alphabetScene.test.ts + navigation.test.ts). Red: 2 failed exactly as predicted. MusicalMemory test PASSED pre-fix, proving playSequence() in create() self-recovers (re-locks + unlocks via delayed calls). Fix: reset inputLocked=false in create() of AlphabetScene and PatternBuilderScene. Green: 778 tests pass.
