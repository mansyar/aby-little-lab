Bug fix: slot letter used setDisplaySize(80,80) then setScale(1.15) - setScale overwrites the 80/512 scale factor, rendering the letter at the full 512px texture size (overflowing the 120px slot). Now sized purely via setDisplaySize(92,92) with the settle tween on displayWidth/displayHeight to 80. Regression test asserts setDisplaySize(92,92), no setScale call, and displayWidth/Height 80 tween targets. 771 tests green.
