Task summary (speech.test.ts): Red-phase unit tests for the TTS utility. Contracts: isSpeechSupported detects speechSynthesis presence; speakLetter returns false and stays silent when disabled (SFX toggle); when enabled speaks a single en-US utterance at rate 0.9 after cancelling prior utterances; never throws and returns false when the API is unavailable. Uses vi.stubGlobal for speechSynthesis/SpeechSynthesisUtterance; happy-dom environment.
