Task: Replace unbounded success particles with lightweight completion feedback.\n\nChanged src/utils/completionEffect.ts and five particle-using game scenes plus MusicalMemoryScene. The shared effect draws one flat ray-of-light splash, uses a 420ms tween (180ms for prefers-reduced-motion), and destroys its Graphics object in onComplete. Removed all add.particles calls, particle emitters, reduced-motion particle-count logic, and the Animal Trace emitter state. Musical Memory now receives the same bounded completion visual. Updated navigation tests to require no particle emitters, a graphics tween with cleanup, bounded duration, and reduced-motion duration.\n\nVerification: pnpm exec vitest run src/__tests__/audio/AudioManager.test.ts src/__tests__/scenes/navigation.test.ts (180 passed); pnpm run check passed; pnpm run build passed with the existing large-chunk warning.
