Task: Hub profile switcher (chip + picker) — Added PROFILE_AVATAR_TEXTURES map in profileLogic.ts (cat/dog/pig/frog/duck/bear -> animal_cat/animal_dog/animal_pig/frog_red/sm_duck/toy_teddy_bear, zero new SVGs). HubScene: createProfileChip() renders active profile avatar top-left with 96x96 hit area (72px display), pointerup opens picker directly (no parental hold); openProfilePicker() draws dim overlay + one >=96px avatar per profile, active scaled 1.15x; tapping a different profile calls switchProfile + rerenderStickerShelf(); overlay tap closes without switching; closeProfilePicker() destroys overlay objects and resets flag (no stale input lock); shutdown handler closes picker and destroys chip. Tests: 4 new in navigation.test.ts (chip renders 96px, picker opens without 3s hold, switch rerenders shelf 20 hasSticker calls, overlay closes without switch). Files: src/scenes/HubScene.ts, src/game/profileLogic.ts, src/__tests__/scenes/navigation.test.ts. Why: Phase 2 of multi-kid-profiles track; 817/817 tests pass, biome clean.
