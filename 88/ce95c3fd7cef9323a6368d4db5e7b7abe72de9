Task: Add deploy job gated to master — deploy job with if: github.event_name == 'push' && github.ref == 'refs/heads/master' (structurally impossible on PR events), needs: quality-gates, fail-fast guard step for missing COOLIFY_DEPLOY_WEBHOOK secret (env-mapped if check, clear ::error + exit 1), webhook POST via curl --fail-with-body. Task: Verify deploy guarding logic — static review passed: job-level condition, needs dependency, secret guard, curl fail semantics; secret name documented in workflow comments. Validated with actionlint 1.7.12: 0 errors.
