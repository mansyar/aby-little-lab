Task: Choreographed win celebration in all six handleComplete flows (Phase 3, cross-cutting-motion_20260801)

Added createWinCelebration(scene, x, y) to completionEffect.ts: star-burst of 10 rays around a bright core (Graphics, no particle emitter) that grows and fades over 700ms, plus 10 colored confetti bits that drift/spin and fade with random stagger. Every object self-destructs via its own tween onComplete. Under reduced motion: 6 rays, no confetti, 300ms, no scale growth. Replaced the uniform 300ms scale-yoyo win tween in ShapeSorter, AnimalTrace, PopFreeze, ShadowMatch, BigSmall, and the splash + frog yoyo in MusicalMemory. Removed orphaned constants (SHAPE_BASE_SCALE, OBJECT_BASE_SCALE, SPRITE_BASE_SCALE, WIN_TWEEN_SCALE). Added 4 unit tests + assertWinCelebrationCreated helper; updated the 3 old yoyo-based win tests. Full suite: 400 tests pass; biome check clean.
