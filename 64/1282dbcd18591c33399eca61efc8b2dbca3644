Task: Update PWA display configuration\n\nSummary: Added manifest display_override metadata requesting fullscreen with standalone fallback and extended the PWA validator to require both modes. The production build generated a manifest with the new metadata and all 13 validator checks passed.\n\nFiles: vite.config.ts; scripts/validate-pwa.js\n\nWhy: The installed mobile PWA was reported not to enter fullscreen; the manifest must request the strongest supported display mode while retaining standalone compatibility.
