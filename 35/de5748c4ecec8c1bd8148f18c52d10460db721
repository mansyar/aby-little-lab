Task 1.3: Apply fontFamily via textStyle() helper to every Text object in app. Wrapped style objects in 11 scenes (back buttons), HubScene (tile labels, Settings button), SettingsPanel (all ~21 sites incl toggles, rows, modals, overlays), PwaToast (title, buttons). Added textStyle helper to typography.ts (returns {fontFamily: FONT_FAMILY, ...style}) + 3 unit tests (baseline, prop passthrough, explicit fontFamily override). Alphabet target/cards deferred to 1.5 (SVG swap), PopFreeze Zzz deferred to 1.4, MusicalMemory emoji deferred to 3.4. Suite: 942 pass, lint unchanged (15 pre-existing warnings).
