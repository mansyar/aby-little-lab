Task: Extend first-words integration coverage for new words. Added 4th integration test: drives Find the Word with injected rounds for OWL (mascot_idle reuse), SUN (new sm_sun SVG), DUCK (new sm_duck SVG), BEAR (toy_teddy_bear reuse) through the real scene + real logic + real storage; asserts each round plays correctly, sticker earned, and prompt images (180x180 setDisplaySize filter) use the right texture keys. Same for Build the Word with DUCK + BEAR. No scene-rule code changes were needed (data-driven scenes; confirmed). Full suite: 29 files / 779 tests green.
