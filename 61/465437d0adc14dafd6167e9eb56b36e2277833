Phase 6: 4 shadow files rebalanced. shadow_airplane: redrawn to match new airplane item (fuselage + nose + tail fin + swept wing). shadow_umbrella: handle 7px->36px->48px (pixel-verified 36px was rendering correctly; bumped to 48px = 13.7% canopy, in 10-15% spec) + fatter hook. shadow_boat: hull deepened (90->125px, spans x32..480), sails trimmed, mast 8->14px. shadow_mushroom: stem widened (92->112px) + extended to y440. ball/car/house/tree unchanged. Vision-approved: 8/8 READY.
