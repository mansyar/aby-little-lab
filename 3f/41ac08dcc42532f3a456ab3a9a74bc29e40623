Hub tile layout repair: root cause = tile entrance pop tween reset icon scale to native 512px (giant glyphs over the grid). Split entrance into tile+label pop (scale 1) and icon pop (ends at its display scale 64/512 or 52/512 for the four typography-heavy tiles tile_alphabet/tile_word_match/tile_word_builder/tile_how_many via GAME_TILES.iconDisplay/iconOffsetY). Replaced vertical bob (4px y, phase-offset) with scale-only breathe wave (1.025, 2500ms, 200ms stagger, starts after entrance) so labels can never move toward icons or the fixed shelf slots. Lowered shelf slot center from +55 to +62 so dashed circles clear label bob range. Added transparent: true to Phaser config - scenes never set a camera bg, opaque canvas rendered black (visible as dark Hub in screenshots). Deleted dead helpers (getCurrentRound/tapCard/fireNextRoundDelay) orphaned by review cleanup; rewrote bob test as breathe test; icon display-size test now asserts per-tile scales. Verified: 972/972 tests, biome clean, build + validate-pwa pass, headless Edge screenshot confirmed all 11 tiles CLEAR (icon/label) via vision subagent.
