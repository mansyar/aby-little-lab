Task 1.5: AlphabetScene now renders letters with preloaded letter_* SVG textures instead of system-font text. Removed TARGET_FONT_SIZE/CARD_FONT_SIZE/LETTER_COLOR, added TARGET_DISPLAY_SIZE=256 (glyph ~200px visual) + CARD_LETTER_DISPLAY_SIZE=128 (~100px visual) + letterKey() helper. cardTexts Text[] -> cardLetters Image[]. renderRound target + createCards use add.image(...letterKey()). Tests: getTargetText/getCardTexts replaced with image-based getTargetLetter/getCardLetters via isLetterKey; render test asserts targetCall key letter_<target>.toLowerCase() and card keys match choices; accumulation destroy check on images; wiggle tests target rect+letter. Suite: 942 pass, lint 15 pre-existing warnings.
