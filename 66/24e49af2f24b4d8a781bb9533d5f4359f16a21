Bug fix discovered during manual verification (2026-08-02): load() returned raw parsed JSON verbatim, so users with a save from before Game 7 shipped crashed at HubScene.create (stickers['pattern-builder'] undefined -> hasSticker threw). Fix: load() now merges saved data over createDefaultStorage() per key (stickers and settings), backfilling missing game ids with unearned entries while preserving existing progress/settings. TDD: red test 'migrates an older save that predates a game by backfilling default sticker entries' -> green after fix. Verified: storage.test.ts 13/13, full suite 592/592, Biome clean.
