Task summary (GameId + storage): Added 'alphabet-match' to GameId union in src/types/index.ts; new default sticker entry in storage.ts createDefaultStorage so load() backfills it for old saves and resetProgress() clears it. Tests: GAME_IDS array extended (defaults + reset assertions now cover Game 8), save/load round-trip fixture updated, new migration test proves a pre-Game-8 save (no alphabet-match key) loads with an unearned alphabet-match entry while preserving existing stickers/settings.
