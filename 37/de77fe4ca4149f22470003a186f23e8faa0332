Review fix (Medium finding): HubScene.stickerImages accumulated stale destroyed references across visits (create() re-runs via scene.start on every return from a game); rerenderStickerShelf() could destroy objects from prior visits. Now cleared in the shutdown handler alongside other per-visit cleanup. Regression test: shutdown + second visit + parental reset re-render must not destroy first-visit stickers, and yields exactly 7 fresh thumbnails. Full suite 663/663 green.
