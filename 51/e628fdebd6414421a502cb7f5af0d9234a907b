Phase 1 Verification Report: Mascot Assets

Automated checks (all green):
- CI=true pnpm test -- --run: 491/491 tests passed (16 files), including updated preload count (46) and new mascot pose key test.
- pnpm run build: success; PWA generateSW precache 7 entries; mascot_idle confirmed inside dist/assets/index-*.js bundle.

Files: mascot_idle.svg, mascot_celebrate.svg (new), PreloadScene.ts (+2 imports/entries), navigation.test.ts (44->46, +1 test).

Manual verification plan (pending user's dev-server check): pnpm dev; confirm no missing-texture warnings; mascot assets present in dist bundle.

Risk/notes: SVGs are inlined into the JS bundle via ?raw imports, so PWA precache covers them automatically (no new public/ assets).
