Task: Initialize pnpm project and install dependencies + Configure TypeScript + Configure Vite + PWA + Configure Biome + Configure Vitest + Create project structure and entry files + Verify scaffolding works

Summary: Established the complete project scaffolding for Aby's Little Lab. Created package.json with pnpm, installed all dependencies (phaser@4.2.1, typescript@7.0.2, vite@8.1.5, vite-plugin-pwa@1.3.0, vitest@4.1.10, @biomejs/biome@2.5.5, happy-dom@18.0.1). Configured TypeScript (ES2023 target, strict mode, bundler resolution, vitest/globals types), Vite with VitePWA plugin (autoUpdate, manifest with landscape orientation, standalone display, precache), Biome linter/formatter (2-space indent, double quotes, semicolons, recommended preset, organizeImports), and Vitest (happy-dom environment, v8 coverage with 80% thresholds). Created index.html entry point, base CSS reset with touch-action:none and user-select:none, minimal main.ts, full directory structure, and placeholder 512x512 PWA icon.

Verification: pnpm run build succeeds (PWA service worker generated), CI=true pnpm test executes (no test files yet - expected), pnpm run check passes with no errors.

Files created: .gitignore, biome.json, index.html, package.json, pnpm-lock.yaml, public/icons/icon-512.png, src/main.ts, src/styles/style.css, tsconfig.json, vite.config.ts, plus .gitkeep files for all source directories.

Why: This scaffolding provides the foundational infrastructure (build, test, lint, PWA) upon which all subsequent game features will be built. Every config follows the tech-stack.md specification exactly.
