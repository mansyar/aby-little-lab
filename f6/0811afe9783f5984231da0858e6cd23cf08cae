Task summary: SettingsPanel constructor accepts injected InstallTracker (defaults to real browser wiring); creates install row at centerY+175 based on tracker state - 'Install App' (installable -> tracker.prompt()) or 'How to Install' (ios-howto -> overlay); hidden state renders nothing. iOS overlay: backdrop + panel + 3 steps + Close, tracked in overlayObjects, close destroys overlay only, panel destroy clears both. Panel height 320->460 for the third row; existing toggle-target test updated to filter by label instead of slice(1). 4 new tests: installable button + prompt call, hit area >=64px, ios overlay opens, hidden renders nothing.
