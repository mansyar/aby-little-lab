Task: Implement reliable first-interaction BGM startup\n\nSummary: Added a shared Hub gesture handler that resumes the AudioContext and starts enabled BGM before entering a game, and attached the same behavior to the settings interaction. Existing parental-lock listener ordering is preserved. Hub navigation tests pass, including settings flow.\n\nFiles: src/scenes/HubScene.ts\n\nWhy: The deployed app resumed audio on tile taps but never called playBGM(), so BGM did not begin after a valid user gesture.
