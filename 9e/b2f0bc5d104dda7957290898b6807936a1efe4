Task: Verify Hub sticker shelf reflects reset. Found gap: shelf rendered once at create(); reset while panel open left it stale. SettingsPanel now accepts onProgressReset callback (invoked after confirmed reset); HubScene passes it and rerenders the shelf (destroys thumbnails, re-reads storage, re-runs entrance animations). Extracted createShelfSticker() from the tile loop; rerenderStickerShelf() recomputes grid positions. Tests: SettingsPanel invokes callback; Hub passes callback and re-renders dimmed shelf after resetProgress(). Updated pre-existing assertion for new constructor contract (scene, undefined, callback).
