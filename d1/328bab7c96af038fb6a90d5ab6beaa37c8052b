Task: Parental-gated profile management in Settings — SettingsPanel: panel height 500->560; new Profiles row (primary color, 240x64 hit) opens a profile manager overlay: one row per profile (96px avatar image + Delete button, 80px apart), Add Profile section with unused avatars (100x100 hit, >=64px), 'Profile limit reached' when 4 profiles exist, Close button; delete requires two-step modal (Delete profile? / Cancel / Delete) in a separate modalObjects array so the overlay stays intact on cancel; confirmDeleteProfile deletes, rebuilds overlay, notifies onProgressReset; overlay rebuilds after add/delete. MAX_PROFILES imported from types. Tests: 9 new in SettingsPanel.test.ts (overlay opens, add makes active + rebuild, used avatars excluded, limit message at 4, two-step confirm/cancel, confirm delete + active stays, last-delete recreates default, parent notified on add+delete, >=64px touch targets); extended mock scene with add.image + setScale. Files: src/components/SettingsPanel.ts, src/__tests__/components/SettingsPanel.test.ts. Why: Phase 3 of multi-kid-profiles track; 826/826 tests pass, biome clean (16 pre-existing warnings).
