Task summary (speech.ts): Minimal Web Speech API wrapper. speakLetter(letter, enabled) — silenced by caller-supplied SFX flag, cancels prior utterances, en-US lang, rate 0.9, sets text explicitly (constructor arg ignored by mocks). isSpeechSupported() presence check. try/catch guarantees no-throw when API unavailable; visual letter display remains the primary cue. One Green-phase fix: assign utterance.text after construction instead of constructor arg.
